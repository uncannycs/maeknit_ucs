<?xml version="1.0" encoding="utf-8"?>
<odoo>
    
    <!-- Success template for Excalidraw OAuth callback -->
    <template id="excalidraw_auth_success" name="Excalidraw Auth Success">
        <t t-call="web.layout">
            <div class="container mt-5">
                <div class="alert alert-success text-center">
                    <h3>Successfully connected to Excalidraw!</h3>
                    <p>You can close this window now.</p>
                </div>
            </div>
            
            <script type="text/javascript">
                // Extract token from URL hash fragment
                const hash = window.location.hash.substring(1);
                const params = new URLSearchParams(hash);
                const token = params.get('access_token');
                const moId = '<t t-esc="mo_id"/>';
                
                if (token &amp;&amp; moId) {
                    // Save token via AJAX
                    fetch('/excalidraw/auth/save_token', {
                        method: 'POST',
                        headers: {
                            'Content-Type': 'application/json',
                        },
                        body: JSON.stringify({
                            jsonrpc: '2.0',
                            method: 'call',
                            params: {
                                mo_id: moId,
                                token: token
                            }
                        })
                    })
                    .then(response => response.json())
                    .then(data => {
                        console.log('Token saved:', data);
                        // Close window after successful save
                        setTimeout(() => window.close(), 2000);
                    })
                    .catch(error => {
                        console.error('Error saving token:', error);
                    });
                } else {
                    console.error('No token or MO ID found in URL');
                }
            </script>
        </t>
    </template>
    
    <!-- Error template for Excalidraw OAuth callback -->
    <template id="excalidraw_auth_error" name="Excalidraw Auth Error">
        <t t-call="web.layout">
            <div class="container mt-5">
                <div class="alert alert-danger text-center">
                    <h3>Excalidraw Authentication Error</h3>
                    <p t-esc="error"/>
                    <p>Please try again or contact your system administrator.</p>
                </div>
            </div>
        </t>
    </template>
    
</odoo>
