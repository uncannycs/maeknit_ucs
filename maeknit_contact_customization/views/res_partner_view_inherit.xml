<odoo>
  <data>
    <record id="view_partner_form_contact_type_custom" model="ir.ui.view">
      <field name="name">res.partner.form.contact_type.custom</field>
      <field name="model">res.partner</field>
      <field name="inherit_id" ref="base.view_partner_form"/>
      <field name="arch" type="xml">

        <xpath expr="//field[@name='company_type']" position="attributes">
          <attribute name="invisible">1</attribute>
        </xpath>

        <!-- Optional: hide is_company -->
        <xpath expr="//field[@name='is_company']" position="attributes">
          <attribute name="invisible">1</attribute>
        </xpath>

        <xpath expr="//sheet//span[@name='address_name']" position="before">
          <field name="contact_type" class="mk-contact-type" colspan="2"/>
          <newline/>
        </xpath>

        <!-- Change default type from 'other' to 'contact' in child contacts/addresses -->
        <xpath expr="//field[@name='child_ids']" position="attributes">
          <attribute name="context">{'default_parent_id': id, 'default_street': street, 'default_street2': street2, 'default_city': city, 'default_state_id': state_id, 'default_zip': zip, 'default_country_id': country_id, 'default_lang': lang, 'default_user_id': user_id, 'default_type': 'contact'}</attribute>
        </xpath>

      </field>
    </record>
  </data>
</odoo>
